--extra-index-url https://download.pytorch.org/whl/nightly/cu128
--pre

# Core ML
torch==2.12.0
torchvision==0.27.0
transformers==4.44.2
dictionary-learning==0.1.0

# LLM Judge (MedGemma 4B via HuggingFace + LangGraph)
# langgraph is consumed by src/evaluate_llm_judge.py; it is not installed in
# the local CPU .venv (the judge runs on a separate GPU host). Left unpinned.
langgraph>=0.2

# Data processing
pandas==3.0.3
numpy==2.4.6
lxml==6.1.1
Pillow==12.2.0

# Analysis & Visualization
scikit-learn==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
scipy==1.17.1

# Dataset download
kagglehub==0.3.13

# Utilities
tqdm==4.67.3
nbformat==5.10.4
pytest==9.0.3
# python-dotenv provides `from dotenv import load_dotenv` (used by evaluate_llm_judge.py).
# Left unpinned: not installed in the local CPU .venv (judge runs elsewhere).
python-dotenv
